##
# Basic auth username and password. It secured with CRYPT.
#
# To generate new credentials:
#  htpassword -d /etc/nginx/.htpassword username
#
# Note that this should not be treated as a way to secure anything. It is not
# secure. Its true purpose is simply to block robots and scrapers from services
# that we do not want access. This is easier than managing IP addresses to
# allow or deny.


